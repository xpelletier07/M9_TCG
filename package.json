{
  "name": "m9-tcg",
  "private": true,
  "workspaces": [
    "app/Client",
    "app/Serveur"
  ],
  "scripts": {
    "test": "npm run build --workspace=client && npm test --workspace=serveur"
  },
  "dependencies": {
    "express-validator": "^7.3.2"
  }
}
